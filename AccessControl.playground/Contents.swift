import Cocoa

/*
 
 # open & public
 - 선언한 모듈이 아닌 다른 모듈에서 사용 가능
 - open은 다른 모듈에서 오버라이드와 서브클래싱이 가능
 - public 다른 모듈에서 오버라이드와 서브클래싱이 불가능
 
 # Internal
 - 기본 접근레벨 (생략 가능)
 - 해당 모듈 전체에서 사용 가능
 
 # File Private
 - 특정 엔티티를 선언한 파일 안에서만 사용 가능
 
 # Private
 특정 엔티티가 선언된 {} 안에서만 사용 가능
 
*/
